class Shery {
    static  version = 1.0;
}

//here without making and instance of class shery we can access version because we have added static 

// Shery.version;  // like this 


//Abstract class are like a base class jinki instance nhi banate sirf property use karte hai.